import { dataSourceStudent, selectedStudentIds } from './student.tsx';
import { dataSourceClass, selectedClassIds } from './class.tsx';
import { dataSourceLicense, selectedLicenseIds } from './license.tsx';

export { dataSourceStudent, selectedStudentIds, dataSourceClass, selectedClassIds, dataSourceLicense, selectedLicenseIds };